# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# node / vite
node_modules
dist
*.local

# OS cruft
.DS_Store

# ChatGPT source renders (large PNGs). Converted WebP copies live in public/assets/media.
assets-src/**/*.png
assets-src/**/*.jpg
assets-src/**/*.webp

# Internal plans and analysis (telemetry, AL notes). The repo is public; keep these local.
docs-internal/
